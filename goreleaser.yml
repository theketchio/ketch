project_name: ketch
before:
  hooks:
    - make generate
builds:
  - id: ketch
    binary: ketch
    main: ./cmd/ketch/
    goos:
      - linux
      - darwin
    goarch:
      - amd64
archives:
  - id: default
    builds:
      - ketch
    format: binary
