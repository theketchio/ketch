{{ range $_, $https := .Values.app.ingress.https }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ $https.secretName }}
spec:
  secretName: {{ $https.secretName }}
  dnsNames:
    - {{ $https.cname }}
  issuerRef:
    name: {{ $https.clusterIssuer }}
    kind: ClusterIssuer
---
{{ end }}