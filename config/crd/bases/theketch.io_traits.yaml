
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: traits.theketch.io
spec:
  group: theketch.io
  names:
    kind: Trait
    listKind: TraitList
    plural: traits
    singular: trait
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: Trait is the Schema for the traits API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: TraitSpec defines the desired state of Trait
          properties:
            appliesToWorkloads:
              items:
                type: string
              type: array
            conflictsWith:
              items:
                type: string
              type: array
            definitionRef:
              properties:
                apiVersion:
                  type: string
                kind:
                  type: string
                name:
                  type: string
              type: object
          required:
          - appliesToWorkloads
          - conflictsWith
          - definitionRef
          type: object
        status:
          description: TraitStatus defines the observed state of Trait
          properties:
            configMapRef:
              description: ConditionedStatus reflects the observed status of a resource
                runtimev1.ConditionedStatus `json:",inline"` ConfigMapRef refer to
                a ConfigMap which contains OpenAPI V3 JSON schema of Component parameters.
              type: string
            latestRevision:
              description: LatestRevision of the component definition
              properties:
                name:
                  type: string
                revision:
                  format: int64
                  type: integer
                revisionHash:
                  description: RevisionHash record the hash value of the spec of ApplicationRevision
                    object.
                  type: string
              required:
              - name
              - revision
              type: object
          type: object
      type: object
  version: v1beta1
  versions:
  - name: v1beta1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
