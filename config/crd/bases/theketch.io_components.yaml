
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: components.theketch.io
spec:
  group: theketch.io
  names:
    kind: Component
    listKind: ComponentList
    plural: components
    singular: component
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: Component is the Schema for the components API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ComponentSpec defines the desired state of Component
          properties:
            schematic:
              description: Schematic
              properties:
                kube:
                  description: Kube is a template with parameter list for a certain
                    Kubernetes workload resource as a component.
                  properties:
                    parameters:
                      items:
                        properties:
                          fieldPaths:
                            items:
                              type: string
                            type: array
                          name:
                            type: string
                          required:
                            type: boolean
                          templateName:
                            type: string
                          type:
                            type: string
                        required:
                        - fieldPaths
                        - name
                        - required
                        - templateName
                        - type
                        type: object
                      type: array
                    templates:
                      description: Templates
                      items:
                        properties:
                          name:
                            type: string
                          object:
                            type: object
                        required:
                        - name
                        - object
                        type: object
                      type: array
                  required:
                  - templates
                  type: object
              required:
              - kube
              type: object
            workload:
              description: Workload represents an identifier to a workload type
              properties:
                default:
                  description: Default is whether the component is applied by default
                    when creating an Application
                  type: boolean
                workloadGVK:
                  description: Definition is a reference to Workload definition via
                    Group, Version, Kind
                  properties:
                    apiVersion:
                      description: APIVersion defines the versioned schema of this
                        representation of an object.
                      type: string
                    group:
                      description: Group defines the object's group
                      type: string
                    kind:
                      description: Kind is a string value representing the REST resource
                        this object represents.
                      type: string
                  type: object
              required:
              - default
              - workloadGVK
              type: object
          required:
          - workload
          type: object
        status:
          description: ComponentStatus defines the observed state of Component
          type: object
      type: object
  version: v1beta1
  versions:
  - name: v1beta1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
